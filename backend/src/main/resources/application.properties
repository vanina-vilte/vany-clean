spring.application.name=demo
server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/vaniclean?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.mail.to=vaninasvilte@gmail.com
app.cors.origin=http://localhost:5173

# JWT (minimo 32 caracteres para HS256; cambiar en produccion)
jwt.secret=clave-super-secreta-para-firmar-tokens-jwt-vaniclean-2026
jwt.expiration=86400000

# Admin por defecto (se crea al arrancar si no existe; cambiar en produccion)
admin.default.email=${ADMIN_EMAIL:admin@vaniclean.com}
admin.default.password=${ADMIN_PASSWORD:VaniClean2026.admin}
